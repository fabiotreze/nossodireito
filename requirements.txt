# NossoDireito - Python Dependencies
# Python 3.10+

# HTTP Client for URL validation
requests~=2.33.1

# HTML/XML parsing for content validation
lxml~=6.0.4

# JSON Schema validation (Draft 7)
jsonschema~=4.26.0
