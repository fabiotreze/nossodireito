# ============================================================
# NossoDireito — Terraform Variables
# ============================================================
# USO LOCAL:
#   Copie para terraform.tfvars e preencha.
#   NÃO commite terraform.tfvars no repositório!
#   Para outro ambiente: terraform apply -var-file="staging.tfvars"
#
# USO VIA GITHUB ACTIONS (CI/CD):
#   As variáveis são injetadas via GitHub Secrets:
#     subscription_id  → secrets.ARM_SUBSCRIPTION_ID
#     pfx_password     → secrets.TF_VAR_pfx_password
#   Demais variáveis usam os defaults definidos em variables.tf.
#   Para alterar defaults no CI, edite variables.tf ou adicione
#   mais -var flags em .github/workflows/terraform.yml.
# ============================================================

subscription_id = "YOUR_AZURE_SUBSCRIPTION_ID"

# --- E-mail para alertas do Azure Monitor ---
# alert_email = "seu-email@example.com"

# --- Ambiente ---
# environment = "prod"     # prod | staging | dev

# --- SKU ---
# app_service_sku = "B1"   # F1 (Free) | B1 ($13/mês, suporta SSL)

# --- Domínio customizado (2 etapas) ---
# Etapa 1: Deploy sem domínio → copie o default_hostname → configure CNAME no DNS
# Etapa 2: Habilite as linhas abaixo e faça terraform apply novamente
#
# custom_domain        = "nossodireito.fabiotreze.com"
# enable_custom_domain = true

# --- Key Vault + Certificado SSL ---
# Habilite para armazenar seu certificado PFX no Azure Key Vault.
# A senha do PFX NUNCA é gravada em disco — informe via:
#   terraform apply -var="pfx_password=MINHA_SENHA"
#   ou: export TF_VAR_pfx_password="MINHA_SENHA"
#
# enable_keyvault = true
# pfx_file_path   = "../fabiotreze-wildcard.pfx"
# app_service_sku = "B1"         # Necessário para SSL próprio ($13/mês)

# --- Região ---
# location = "brazilsouth"

# --- Tags extras ---
# extra_tags = {
#   owner = "fabio"
# }
