# OS
.DS_Store
Thumbs.db
desktop.ini

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Certificates / Secrets
*.pfx
*.pem
*.key
*.p12
*.crt
*.cer
*.jks
*.env
.env*
secrets/
credentials/
*.secret
auth.json
service-principal*.json
azure-credentials*.json
gsc_oauth_client.json

# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/*.tfvars
!terraform/*.tfvars.example
# terraform/.terraform.lock.hcl  ← committed on purpose (provider supply-chain pinning)
terraform/crash.log
terraform/.terraform.tfstate.lock.info
*.tfplan
*.tfplan.json

# Temporary
*.tmp
*.log
temp_*
_temp_*

# Build artifacts
index.min.html
.coverage
htmlcov/
.pytest_cache/

# Generated reports (re-created by scripts)
quality_report.json
validation_report.json
validation_legal_report.json
audit_dicas_report.json
audit_dead_urls_report.json
audit_coverage_report.json
audit_prescriptive_language.json
data/discovery_report.json
data/discovery_report.md
screenshots/

# Pre-rendered SEO HTML (gerado por scripts/prerender_direitos.py)
direitos/*/index.html

# Node.js
node_modules/

# Backups temporários
backup/
backups/
*.backup
index.backup_*

# Plan binaries — nunca commitar
terraform/tfplan*
terraform/*.tfplan

# Pre-compressed static siblings (gerados por scripts/precompress_static.mjs no deploy)
*.br
*.gz

# Lighthouse CI outputs (local runs)
.lighthouseci/

# Output do scanner local (regenerado a cada `node scripts/security_audit.mjs`)
security_audit_results.json
